{% extends 'base.html.twig' %}

{% block title %} Modifier {{ task.title }} {% endblock %}

{% block header_title %}
    <h1> Modifier la tâche : {{ task.title }} </h1>
{% endblock header_title %}

{% block header_img %}<img class="w-100" src="{{ asset('img/todolist_content.jpg') }}" alt="todo list">{% endblock %}

{% block body %}
    <div class="row mt-3">
        <div class="col-12">
            {{ include('task/elements/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
    </div>

    <div class="d-flex">
        <a href="{{ path('app_task_index') }}" class="btn btn-sm btn-secondary mx-1"> Retour </a>
        {{ include('task/elements/_delete_form.html.twig') }}
    </div>
{% endblock %}
